#include "Halt.h"

Halt::Halt(){}
Halt::~Halt(){}

int Halt::execute(vector<StackValues*> &rstack, vector<int> &fpstack, int &sp, int &fpsp, int pc, int &flag){

	flag = 0;
	// cout << flag << endl;

	return pc;
}
